# VeppStatus Project Dependencies
# Django веб-приложение - обновлено для совместимости с современными версиями Python
Django==2.2.28
zerorpc
pyzmq
psycopg2-binary
psutil

# Дополнительные зависимости для веб-интерфейса
# (уже включены в статические файлы проекта)
# jquery, jqwidgets, zingchart - загружаются через CDN

# Зависимости для сервера сбора данных (опционально)
# Примечание: Эти пакеты требуют дополнительной настройки системы
# PyQt4 - устанавливается через системный пакетный менеджер
# cothread - требует EPICS (установка через install_epics.sh)
# pycx4 - требует EPICS (установка через install_epics.sh)

# Для разработки (опционально)
# ipython
# pytest
# flake8